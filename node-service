apiVersion: v1
kind: Service
metadata:
  name: node-service
spec:
  type: NodePort
  selector:
    app: node
  ports:
    - port: 3000        # Service port
      targetPort: 3000  # Port in the container
      nodePort: 30080   # External port (optional, can be any between 30000–32767)
